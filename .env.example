# Variables de entorno para eMeet Frontend
# Copia este archivo como .env.local y completa los valores reales.
# NUNCA subas .env.local a git.

# ─── Supabase ────────────────────────────────────────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=https://ksghpwonmnxmbhmfpaog.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# ─── Backend API (requerido para arquitectura separada) ──────────────────────
# URL base del backend Express (ej: http://localhost:4000)
NEXT_PUBLIC_BACKEND_URL=http://localhost:4000

# ─── Prisma / PostgreSQL ─────────────────────────────────────────────────────
DATABASE_URL=your_supabase_postgres_connection_string

# ─── Google Maps ─────────────────────────────────────────────────────────────
# Obtén tu API key en: https://console.cloud.google.com/apis/credentials
# Activa el API: Maps JavaScript API
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here
