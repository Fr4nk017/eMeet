{
  "name": "emeet-monorepo",
  "private": true,
  "version": "0.0.0",
  "packageManager": "npm@11.6.2",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "start": "turbo run start"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  }
}
